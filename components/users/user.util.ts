export enum DynamicSyncTypeEnum {
  reverso = "reverso",
  google = "google",
}

export type DynamicSyncDataType = {
  email?: string;
  password?: string;
  accountName?: string;
};

export enum DeckPositionEnum {
  up = "up",
  down = "down",
}
