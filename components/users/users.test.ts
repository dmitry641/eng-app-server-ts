describe("UserStore tests:", () => {
  beforeAll(async () => {
    // await connectToDB();
  });

  describe("Create user", () => {
    // it.todo("...");
    it("with valid data", () => {
      expect(1).toBe(1);
    });
  });
});
